#pragma once

namespace function
{
double calculateAngle(int x, int y, double radius);
double calculateSpinVX(double range, double radius, double speedFactor, double angle);
double calculateSpinVY(double range, double radius, double speedFactor, double angle);
double **fillVDistributionX(double spinCenterX, double spinCenterY);
double **fillVDistributionX(double spinCenterX, double spinCenterY);
}